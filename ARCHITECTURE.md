# 🏗️ Arquitetura de Protected Routes

## Diagrama de Fluxo Completo

```
┌──────────────────────────────────────────────────────────────────┐
│                         main.tsx                                 │
│                    (Entry Point)                                 │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────────────┐
│                        App.tsx                                   │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │            QueryClientProvider (React Query)              │  │
│  │  ┌──────────────────────────────────────────────────────┐  │  │
│  │  │          AuthProvider (Context API)                 │  │  │
│  │  │  ┌────────────────────────────────────────────────┐  │  │  │
│  │  │  │       TooltipProvider (shadcn/ui)            │  │  │  │
│  │  │  │  ┌──────────────────────────────────────────┐  │  │  │  │
│  │  │  │  │    BrowserRouter (React Router)         │  │  │  │  │
│  │  │  │  │  ┌────────────────────────────────────┐  │  │  │  │  │
│  │  │  │  │  │         Routes                    │  │  │  │  │  │
│  │  │  │  │  │  ┌──────────────────────────────┐  │  │  │  │  │  │
│  │  │  │  │  │  │  PUBLIC ROUTES              │  │  │  │  │  │  │
│  │  │  │  │  │  │  ├─ / (Index)               │  │  │  │  │  │  │
│  │  │  │  │  │  │  ├─ /auth (Auth)            │  │  │  │  │  │  │
│  │  │  │  │  │  │  ├─ /c/:slug (Checkout)     │  │  │  │  │  │  │
│  │  │  │  │  │  │  └─ ... (mais públicas)     │  │  │  │  │  │  │
│  │  │  │  │  │  └──────────────────────────────┘  │  │  │  │  │  │
│  │  │  │  │  │  ┌──────────────────────────────┐  │  │  │  │  │  │
│  │  │  │  │  │  │  PROTECTED ROUTES           │  │  │  │  │  │  │
│  │  │  │  │  │  │  ┌──────────────────────┐   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ ProtectedRoute       │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ ┌────────────────┐   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ │ /dashboard     │   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ │ /products      │   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ │ /checkouts     │   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ │ /sales         │   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ │ ... (mais)     │   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  │ └────────────────┘   │   │  │  │  │  │  │  │
│  │  │  │  │  │  │  └──────────────────────┘   │  │  │  │  │  │  │
│  │  │  │  │  │  └──────────────────────────────┘  │  │  │  │  │  │
│  │  │  │  │  └────────────────────────────────────┘  │  │  │  │  │
│  │  │  │  └──────────────────────────────────────────┘  │  │  │  │
│  │  │  └────────────────────────────────────────────────┘  │  │  │
│  │  └──────────────────────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Fluxo de Verificação de Autenticação

```
┌─────────────────────────────────────────────────────────────┐
│  Usuário tenta acessar rota protegida                       │
│  Exemplo: /dashboard                                        │
└────────────────┬────────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────────┐
│  ProtectedRoute Component                                   │
│  ├─ Verifica isLoading                                      │
│  └─ Verifica isAuthenticated                                │
└────────────────┬────────────────────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
    ┌────────┐        ┌──────────┐
    │Loading │        │Verificar │
    │        │        │Sessão    │
    └────────┘        └────┬─────┘
                           │
                    ┌──────┴──────┐
                    │             │
                    ▼             ▼
            ┌──────────────┐  ┌─────────────┐
            │Autenticado   │  │Não Auth     │
            │              │  │             │
            │Renderiza     │  │Redireciona  │
            │Componente    │  │para /auth   │
            └──────────────┘  └─────────────┘
```

---

## Estrutura de Dados - AuthContext

```typescript
AuthContext
├── user: User | null
│   ├── id: string
│   ├── email: string
│   ├── user_metadata: object
│   └── ... (mais campos)
├── isLoading: boolean
├── isAuthenticated: boolean
└── signOut: () => Promise<void>
```

---

## Fluxo de Login

```
┌──────────────────────────────────────────────────────────────┐
│  Usuário acessa /auth                                        │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  Auth Component                                              │
│  ├─ useAuthContext verifica isAuthenticated                  │
│  └─ Se true → redireciona para /dashboard                    │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  Formulário de Login                                         │
│  ├─ Email                                                    │
│  ├─ Senha                                                    │
│  └─ Botão "Entrar"                                           │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  supabase.auth.signInWithPassword()                          │
│  └─ Envia credenciais para Supabase                          │
└────────────────┬─────────────────────────────────────────────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
    ┌────────┐        ┌──────────┐
    │Sucesso │        │Erro      │
    │        │        │          │
    │Sessão  │        │Toast     │
    │criada  │        │error     │
    └────┬───┘        └──────────┘
         │
         ▼
┌──────────────────────────────────────────────────────────────┐
│  onAuthStateChange dispara                                   │
│  ├─ AuthContext atualiza user                                │
│  ├─ isAuthenticated = true                                   │
│  └─ Redireciona para /dashboard                              │
└──────────────────────────────────────────────────────────────┘
```

---

## Fluxo de Logout

```
┌──────────────────────────────────────────────────────────────┐
│  Usuário clica em "Sair"                                     │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  handleLogout()                                              │
│  ├─ Chama signOut()                                          │
│  └─ Aguarda conclusão                                        │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  supabase.auth.signOut()                                     │
│  └─ Limpa sessão no Supabase                                 │
└────────────────┬─────────────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────────────────────────────┐
│  onAuthStateChange dispara                                   │
│  ├─ AuthContext atualiza user = null                         │
│  ├─ isAuthenticated = false                                  │
│  └─ Redireciona para /auth                                   │
└──────────────────────────────────────────────────────────────┘
```

---

## Hierarquia de Componentes

```
App
├── QueryClientProvider
│   └── AuthProvider
│       └── TooltipProvider
│           ├── Toaster (shadcn/ui)
│           ├── Sonner (Toast notifications)
│           └── BrowserRouter
│               └── Routes
│                   ├── Route (/)
│                   ├── Route (/auth)
│                   ├── Route (/c/:slug)
│                   ├── Route (/dashboard)
│                   │   └── ProtectedRoute
│                   │       └── Dashboard
│                   ├── Route (/products)
│                   │   └── ProtectedRoute
│                   │       └── Products
│                   ├── Route (/checkouts)
│                   │   └── ProtectedRoute
│                   │       └── Checkouts
│                   └── ... (mais rotas)
```

---

## Fluxo de Dados - useAuth Hook

```
useAuth Hook
│
├─ useEffect (ao montar)
│  ├─ checkUser()
│  │  └─ supabase.auth.getSession()
│  │     └─ setUser(session?.user)
│  │
│  └─ supabase.auth.onAuthStateChange()
│     └─ Escuta mudanças de autenticação
│
└─ Retorna
   ├─ user: User | null
   ├─ isLoading: boolean
   ├─ isAuthenticated: boolean
   └─ signOut: () => Promise<void>
```

---

## Fluxo de Dados - AuthContext

```
AuthContext
│
├─ Criado com createContext()
│
├─ AuthProvider (Componente)
│  ├─ Usa useAuth Hook
│  └─ Fornece valor via Context.Provider
│
└─ useAuthContext (Hook)
   ├─ useContext(AuthContext)
   └─ Retorna valores do contexto
```

---

## Sequência de Inicialização

```
1. App.tsx carrega
   ↓
2. QueryClientProvider inicializa
   ↓
3. AuthProvider inicializa
   ├─ useAuth Hook executa
   ├─ Verifica sessão no Supabase
   └─ Escuta mudanças de autenticação
   ↓
4. TooltipProvider inicializa
   ↓
5. BrowserRouter inicializa
   ↓
6. Routes renderizam
   ├─ Rotas públicas renderizam normalmente
   └─ Rotas protegidas
      ├─ ProtectedRoute verifica autenticação
      ├─ Se autenticado → renderiza componente
      └─ Se não → redireciona para /auth
```

---

## Estados Possíveis

```
Estado 1: Carregando
├─ isLoading = true
├─ user = null
├─ isAuthenticated = false
└─ Renderiza: Loading spinner

Estado 2: Autenticado
├─ isLoading = false
├─ user = { id, email, ... }
├─ isAuthenticated = true
└─ Renderiza: Componente protegido

Estado 3: Não autenticado
├─ isLoading = false
├─ user = null
├─ isAuthenticated = false
└─ Renderiza: Redireciona para /auth
```

---

## Integração com Supabase

```
App
│
└─ AuthContext
   │
   └─ useAuth Hook
      │
      └─ supabase client
         │
         ├─ supabase.auth.getSession()
         ├─ supabase.auth.signInWithPassword()
         ├─ supabase.auth.signUp()
         ├─ supabase.auth.signOut()
         └─ supabase.auth.onAuthStateChange()
```

---

## Proteção em Camadas

```
Camada 1: Roteamento
├─ ProtectedRoute verifica autenticação
└─ Redireciona se não autenticado

Camada 2: Contexto
├─ AuthContext fornece estado global
└─ Qualquer componente pode acessar

Camada 3: Componente
├─ useAuthContext verifica permissões
└─ Renderiza conteúdo condicional

Camada 4: Backend
├─ Supabase verifica sessão
└─ Rejeita requisições não autenticadas
```

---

## Segurança

```
✅ Verificação de Sessão
   └─ Ao carregar a aplicação

✅ Redirecionamento Automático
   └─ Se não autenticado

✅ Escuta em Tempo Real
   └─ Monitora mudanças de autenticação

✅ Logout Seguro
   └─ Limpa sessão via Supabase

✅ Proteção de Rotas
   └─ Todas as rotas administrativas protegidas

✅ localStorage com persistência
   └─ Mantém sessão entre recarregamentos
```

---

## Performance

```
Otimizações Implementadas:

✅ Lazy Loading
   └─ Rotas carregadas sob demanda

✅ Caching
   └─ Sessão armazenada em localStorage

✅ useEffect Otimizado
   └─ Evita re-renders desnecessários

✅ Context API
   └─ Evita prop drilling

✅ Memoização
   └─ Valores do contexto não mudam desnecessariamente
```

---

## Próximas Melhorias Arquiteturais

```
[ ] Role-Based Access Control (RBAC)
    └─ Diferentes níveis de acesso

[ ] Refresh Token Rotation
    └─ Segurança aprimorada

[ ] Audit Logging
    └─ Rastreamento de ações

[ ] 2FA (Two-Factor Authentication)
    └─ Segurança adicional

[ ] Session Timeout
    └─ Logout automático após inatividade

[ ] Rate Limiting
    └─ Proteção contra brute force

[ ] CSRF Protection
    └─ Proteção contra ataques

[ ] Error Boundary
    └─ Tratamento de erros global
```
